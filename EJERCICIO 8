#include <stdio.h>
#include <math.h>
int main() 
{
  float x, y;
  // Ciclo for para calcular los valores de y
  for (x = 1.0; x <= 10.0; x += 1.0) {
    y = (3.0 * x + pow(x, 4.0)) + sin(1.0 / x);
    printf("x = %.1f, y = %.2f\n", x, y);
  }
  return 0;
}
